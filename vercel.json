{
  "installCommand": "npm install --production",
  "buildCommand": "mkdir -p public && cp -r !(public|node_modules|.git|.vercel) public/ 2>/dev/null || rsync -av --exclude='public' --exclude='node_modules' --exclude='.git' --exclude='.vercel' . public/",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/api/auth",
      "destination": "/api/auth.js"
    },
    {
      "source": "/api/callback",
      "destination": "/api/callback.js"
    }
  ],
  "headers": [
    {
      "source": "/admin/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache"
        }
      ]
    }
  ]
}
